export function notFoundWithID(model: string) {
    return `${model} with the given id is not found`
}

export function notFoundWithFilters(model: string) {
    return `${model} with the given filters not found`
}